builder:
  port: 80
  env:
    - NAVER_CLIENT_ID
    - NAVER_CLIENT_SECRET

startCommand:
  args: ["python", "main.py"]   # ← 객체(맵) 안에 배열을 넣어야 합니다

transport:
  type: http
  url: http://localhost:80/mcp

runtime:
  memory: 512Mi
  cpu: 0.25
