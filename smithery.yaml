builder:
  port: 80
  env:
    - NAVER_CLIENT_ID
    - NAVER_CLIENT_SECRET

startCommand:
  type: http
  commandFunction: ["python", "main.py"]

transport:
  type: http
  url: http://localhost:80/mcp
  anonymous: true       
  http:
    anonymous: true       

runtime:
  memory: 512Mi
  cpu: 0.25
